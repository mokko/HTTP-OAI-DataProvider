This direction functions as a test environment. Files here contain a working configuration of the data provider using the data I have. The data is in a format called MPX. It's not an integral part of the data provider, but merely an example format.
